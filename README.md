# Homebrew Tap for Conductor

Install the Conductor CLI:

```bash
brew install lovemoon-ai/tap/conductor
```

Upgrade Conductor:

```bash
brew upgrade lovemoon-ai/tap/conductor
```

This tap is updated from the Conductor release workflow. Formula versions and
checksums should come from the generated `conductor.rb` release asset in
`lovemoon-ai/conductor`.
